[
  {
    "id": "complete-run",
    "claim": "A complete run lasts six 30-second tracks and reaches a win or loss screen.",
    "where": "first screen, How it works, README",
    "test": "npm run test:e2e -- --grep @claim:complete-run",
    "sandbox": "fresh browser context at /demo?qa=1; deterministic simulation; assert six tracks, 180000 ms, five choices, and visible win screen"
  },
  {
    "id": "three-misses",
    "claim": "Three missed phrases end a run.",
    "where": "game goal, How it works, README",
    "test": "npm run test:e2e -- --grep @claim:three-misses",
    "sandbox": "fresh browser context at /demo?qa=1; advance the deterministic game without input; assert exactly three misses and visible loss screen"
  },
  {
    "id": "restart-reset",
    "claim": "Play again resets the track, score, misses, and run changes.",
    "where": "end screen, README",
    "test": "npm run test:e2e -- --grep @claim:restart-reset",
    "sandbox": "finish the deterministic sample; use Play again; inspect the new observable game state"
  },
  {
    "id": "settings-persist",
    "claim": "Real-run sound, assist, and remapped key settings stay in this browser.",
    "where": "settings dialog, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:settings-persist",
    "sandbox": "leave demo for a real run; change all three setting types; reload in the same clean browser context; verify controls restore"
  },
  {
    "id": "local-play-data",
    "claim": "Real and sample play data stays in this browser. Gameplay sends no data to another origin.",
    "where": "first-screen fact, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-play-data",
    "sandbox": "record every request through a real finish and restart, then a sample finish; assert requests are same-origin, real storage remains local, and the sample leaves it unchanged"
  },
  {
    "id": "demo-isolation",
    "claim": "One click opens a seeded sample with a persistent Demo label. Reset starts it over without reading or changing real-run storage.",
    "where": "demo banner, privacy page, demo documentation",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh home page; enter the sample in one click, seed real storage with a sentinel, lose and reset the sample, then assert its label and prior result remain visible, the sentinel is unchanged, and sample state resets"
  },
  {
    "id": "one-time-offer",
    "claim": "Pulse Run Complete costs $5 USD once and is not a subscription.",
    "where": "first-screen fact, pricing, license page, terms, public offer metadata",
    "test": "npm run test:e2e -- --grep @claim:one-time-offer",
    "sandbox": "fetch public offer metadata and compare the price type, amount, currency, deliverables, and unavailable checkout state with the visible offer"
  },
  {
    "id": "input-modes",
    "claim": "Notes can be played with the keyboard and the four touch controls.",
    "where": "first-screen fact, game controls, README",
    "test": "npm run test:e2e -- --grep @claim:input-modes",
    "sandbox": "place deterministic playable notes, press the mapped keyboard key and touch control, and assert both increase score"
  },
  {
    "id": "remappable-input",
    "claim": "Every lane key can be remapped and played.",
    "where": "How it works, settings dialog, README",
    "test": "npm run test:e2e -- --grep @claim:remappable-input",
    "sandbox": "fresh real run; remap all four lane keys through settings, play a deterministic note in each lane, and assert each input increases score"
  },
  {
    "id": "free-run-changes",
    "claim": "The free game includes one six-track run, one Circuit percussion set, and six run changes. Each change affects play.",
    "where": "How it works, price section, terms, README",
    "test": "npm run test:e2e -- --grep @claim:free-run-changes",
    "sandbox": "fresh sample; apply each of six real change choices through the game engine and assert its timing, protection, score, controls, note count, or note pattern outcome while the free groove remains Circuit"
  },
  {
    "id": "no-account-ads-tracking",
    "claim": "Pulse Run, including Complete, starts without an account, ads, analytics, tracking pixels, or third-party scripts.",
    "where": "first-screen copy, scope section, pricing, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:no-account-ads-tracking",
    "sandbox": "fresh real run and Complete availability page; start playing without an identity form or popup and record all browser requests to assert only same-origin resources load"
  },
  {
    "id": "audio-gesture",
    "claim": "Percussion starts only after a play control or rhythm key. Pulse Run does not record you.",
    "where": "privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:audio-gesture",
    "sandbox": "fresh sample with a getUserMedia spy; assert audio is locked before input, unlocks after a rhythm key, and never requests media"
  },
  {
    "id": "assist-timing",
    "claim": "A wider timing setting reduces the precision required.",
    "where": "settings dialog, README",
    "test": "npm run test:e2e -- --grep @claim:assist-timing",
    "sandbox": "fresh real run; miss a note just outside the normal window, enable wider timing, then hit the same offset and assert score increases"
  },
  {
    "id": "delete-play-data",
    "claim": "The privacy control deletes all Pulse Run play data from this browser.",
    "where": "privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:delete-play-data",
    "sandbox": "fresh browser storage seeded with all three real-data keys; use the visible privacy control and assert storage is empty with a deletion status"
  },
  {
    "id": "built-in-scope",
    "claim": "Pulse Run is a single-player built-in game with no song uploads, user charts, rankings, or multiplayer rooms.",
    "where": "scope section, README",
    "test": "npm run test:e2e -- --grep @claim:built-in-scope",
    "sandbox": "fresh real run; begin playing, assert no upload or ranking/room UI is available, and record that no WebSocket connection opens"
  },
  {
    "id": "frame-rate",
    "claim": "The game targets 60 fps and measures at least 50 fps in a phone-sized browser under 4x CPU throttling.",
    "where": "design thesis and handoff",
    "test": "npm run test:e2e -- --grep @claim:frame-rate",
    "sandbox": "390x844 Chromium viewport, 4x CPU throttling, active seeded sample, two-second requestAnimationFrame measurement"
  }
]
